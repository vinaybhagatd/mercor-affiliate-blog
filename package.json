{
  "name": "mercor-affiliate-blog",
  "version": "1.0.0",
  "scripts": {
    "eleventy": "npx @11ty/eleventy",
    "build": "eleventy",
    "serve": "eleventy --serve",
    "pipeline": "npx openclaw agent exec ChecksPipelineAgent",
    "pipeline:serve": "npm-run-all --continue-on-error pipeline serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "luxon": "^3.7.2",
    "npm-run-all": "^4.1.5"
  }
}