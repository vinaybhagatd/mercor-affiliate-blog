{% extends "base.njk" %}

{% block content %}
  <h1>{{ title }}</h1>
  <ul>
    {% for post in collections[page.fileSlug] %}
      <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
    {% endfor %}
  </ul>
{% endblock %}
